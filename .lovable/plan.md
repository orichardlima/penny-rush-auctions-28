
O usuário quer uma explicação didática de como funciona o **Gerente de Afiliados**. Não é tarefa de implementação — é pergunta conceitual/documentação. Vou ler a memória detalhada e o código para garantir precisão, e responder direto sem plano de implementação.

Já tenho contexto suficiente da memória `mem://features/affiliate/manager-influencer-hierarchy` e dos hooks (`useAffiliateManager.ts`). Vou verificar rapidamente o trigger de override commission e a UI relevante para garantir os números exatos.
